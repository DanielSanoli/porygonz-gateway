server:
  port: 8080

spring:
  application:
    name: porygonz-gateway

  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          "[/**]":
            allowedOrigins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200}
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

      routes:
        - id: gymleader-pokemon
          uri: ${GYMLEADER_URI:http://gym-leader:8081}
          predicates:
            - Path=/api/v1/pokemon/**

        - id: typematch-compare
          uri: ${TYPEMATCH_URI:http://typematch:8082}
          predicates:
            - Path=/api/v1/compare/**

        - id: fusion-ia-fusions
          uri: ${FUSION_URI:http://localhost:8000}
          predicates:
            - Path=/api/v1/fusions/**